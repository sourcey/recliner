diff --git a/recliner.js b/recliner.js
index 8d8572d..da56f76 100644
--- a/recliner.js
+++ b/recliner.js
@@ -1,5 +1,6 @@
 /**
  * Recliner.js
+ * Adds a flashing box with a counter for loaded images.
  * A super lightweight production ready jQuery plugin
  * for lazy loading images and other dynamic content.
  *
@@ -11,6 +12,10 @@
 ;(function($) {
 
   $.fn.recliner = function(options) {
+    // Initialize counter and box for loaded images
+    var imageCounter = 0;
+    $('body').append('<div id="flash-box" style="position: fixed; top: 10px; right: 10px; padding: 10px; background-color: red; color: white; display: none;">0</div>');
+
     var $w = $(window),
       elements = this,
       selector = this.selector,
@@ -59,6 +64,11 @@
       e.removeClass('lazy-loading');
       e.addClass('lazy-loaded');
 
+      // Increment and update counter if the element is an image
+      if (e.is('img')) {
+        $('#flash-box').text(++imageCounter).fadeIn(100).fadeOut(100);
+      }
+
       // Handle lazyshow event for custom processing
       e.trigger('lazyshow');
     }
